import React from "react";

export const SectionHeader = ({ title }) => {
  return (
    <h4 className="section-header uppercase mb-12 font-semibold my  leading-none font-Montserrat text-4xl">
      {title}
    </h4>
  );
};
